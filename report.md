# Day 1 作业报告：泰坦尼克号幸存者预测

## 1. 本日问题

- 里程碑：day-01
- 小组：张清波组
- 成员：张清波 U202412700（组长）、汤家齐 U202412691、吕绍康 U202412686
- 使用者：准备 Titanic 历史数据教学展项的海事博物馆教育团队
- 真实输入：Kaggle 指定来源的 `train.csv`，共 891 条真实乘客记录
- 需要的输出：同一留出测试集上的多数类基线与随机森林结果、指标、混淆矩阵和真实误判记录
- 与使用者相关的错误：假阴性，即真实标签为幸存但模型预测为未幸存；同时也检查假阳性
- 产品边界：这是历史观察数据分析，不是现代救援分配工具，也不能证明某个特征导致生存

## 2. 真实数据

- 所有者/发布者：Kaggle 用户 `hesh97`
- 标题：Titanic-Dataset (train.csv)
- 原始 URL：https://www.kaggle.com/datasets/hesh97/titanicdataset-traincsv
- 许可标签：CC0: Public Domain
- 取得与核验日期：2026-08-18
- 预期文件与结构：仓库本地 `data/raw/train.csv`；891 行、12 列
- 检查命令：`python train.py --check-data`
- 实际检查结果：`REAL DATA CHECK PASSED`；891 行、12 列；`Survived` 的 0/1 数量为 549/342
- 缺失值：`Age` 177、`Cabin` 687、`Embarked` 2

数据检查能证明文件结构、行数、必要列和标签数量与课程指定文件一致，但不能证明数据没有历史偏差，也不能把记录中的关联解释为因果。原始下载文件由 `.gitignore` 排除，不进入 GitHub 仓库。

## 3. 可复现运行

以下命令均在仓库根目录 `G:\summer工训\day-01-titanic` 运行：

```powershell
# 安装
python -m pip install -r requirements.txt

# 数据检查
python train.py --check-data

# 测试
python -m unittest discover -s tests -v

# 主程序
python train.py

# Git 文本检查
git diff --check
```

实际环境：Python 3.12.4、pandas 3.0.5、scikit-learn 1.9.0。数据检查通过；3 个测试全部通过并显示 `OK`。主程序生成仓库本地的 `metrics.json` 和 `errors.csv`，两者都是可重新生成的运行输出，因此不提交到 Git。

## 4. 基线与候选

### 简单基线

基线使用 `DummyClassifier(strategy="most_frequent")`，总是预测训练集中最常见的 `Survived` 类别。它不学习复杂规律，适合检查候选模型是否真的比“永远猜多数类”更有用。运行命令为 `python train.py`。

### 候选方法

候选方法是在已有预处理流水线后加入 `RandomForestClassifier(random_state=42)`。保持不变的条件包括：7 个输入特征、75%/25% 分层划分、随机种子 42、缺失值处理、类别编码、测试集和评估指标。训练集有 668 行，测试集有 223 行。

| 指标 | 多数类基线 | 随机森林候选 | 含义 |
| --- | ---: | ---: | --- |
| 准确率 | 61.4% | 74.4% | 全部 223 条测试记录中预测正确的比例 |
| 幸存者精确率 | 0.0% | 68.4% | 预测为幸存的记录中，实际幸存的比例 |
| 幸存者召回率 | 0.0% | 62.8% | 86 条真实幸存记录中，被识别出的比例 |
| 幸存者 F1 | 0.000 | 0.655 | 精确率与召回率的综合指标 |
| 混淆矩阵 | `[[137, 0], [86, 0]]` | `[[112, 25], [32, 54]]` | 标签顺序为 0、1 |

候选模型在相同测试集上正确识别出 54 名真实幸存者，漏掉 32 名；基线漏掉全部 86 名真实幸存者。候选模型另有 25 个假阳性。结果支持“本次固定划分中候选模型比多数类基线更有用”，不支持对其他数据或因果关系作保证。

## 5. 一个真实失败案例

- 样本编号：PassengerId 502，`Canavan, Miss. Mary`
- 真实结果：`Survived = 0`
- 系统输出：`predicted_survived = 1`
- 可观察字段：`Pclass = 3`、`Sex = female`、`Age = 21`、`SibSp = 0`、`Parch = 0`、`Fare = 7.75`、`Embarked = Q`
- 说明的限制：候选模型会把部分实际未幸存记录误判为幸存，单条预测不能当成历史事实解释
- 不能证明什么：这些字段不能证明该乘客为何未幸存，也不能据此编写关于舱位位置、救生艇或个人经历的故事
- 下一项最小检查：尚未执行；分别统计 32 个假阴性和 25 个假阳性在 `Pclass`、`Sex` 上的数量，检查错误是否集中，再决定是否需要新特征或不同模型

## 6. 智能体与学生工作边界

- 组员仓库中已有的核心代码改动是完成 `build_candidate()`，在既有预处理后加入固定随机种子 42 的随机森林；本次整理没有改动 `train.py` 或削弱测试。
- 智能体按用户明确请求补齐 README、报告、PPT、成员清单和提交清单，并执行数据、指标、版面、Git 与安全一致性检查。
- 报告中的数字来自实际命令输出；数据来源、许可、行数、缺失值、混淆矩阵和失败记录均经过复核，没有根据模型常识补写乘客故事。
- 本报告不声称三名成员已经完成口头复述。答辩前，每名成员仍需独立解释 `build_candidate()`、流水线、混淆矩阵、PassengerId 502 的证据边界和下一项检查。

## 7. 结论与限制

在固定种子 42 的同一批 223 条留出记录上，随机森林的准确率为 74.4%，高于多数类基线的 61.4%。基线没有识别出任何真实幸存者，而候选模型的幸存者召回率为 62.8%，F1 为 0.655。候选模型仍漏掉 32 名真实幸存者，并产生 25 个假阳性，因此不能只报告准确率。PassengerId 502 的误判说明单条模型输出不能代替历史事实，也不能生成缺少来源的个人故事。数据只记录一次 1912 年历史事件，存在缺失值和历史偏差，不能代表现代人群。当前实验只检查一个数据划分和一个候选模型，没有证明结果对其他随机种子或其他数据稳定。最小证据只支持本次实验中候选模型比多数类基线更有用，不能证明任何特征导致生存，也不能用于真实救援决策。

## 8. 提交复核

- [x] README 包含环境、数据、安装、检查、运行、测试、预期结果和恢复步骤
- [x] 数据检查、3 个测试和主程序重新运行
- [x] 报告数字与 `metrics.json`、`errors.csv` 一致
- [x] `presentation.pptx` 为 4 页，按 3 分钟结构组织并完成版面检查
- [x] `submission.json` 路径正确
- [x] Git 暂存内容不含原始数据、密钥、虚拟环境或缓存
- [ ] 三名成员亲自完成 3 分钟计时排练和教师追问练习
- [ ] 学生在 GitHub 网页复查新仓库 URL，并按教师要求发送 URL
